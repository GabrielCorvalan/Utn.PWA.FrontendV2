import { Routes } from '@angular/router';
import { InternComponent } from './intern.component';
const internRoutes: Routes = [
    { path: 'intern', component: InternComponent }
];

export const internRouting = internRoutes;
